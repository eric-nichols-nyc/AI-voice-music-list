import Hal from "./Hal";

const AnimationManager = () => {
  return (
    <div className="flex h-full items-center justify-center">
      <div className="min-h-0 w-full max-w-2xl rounded-lg border border-border bg-card">
        <Hal />
      </div>
    </div>
  );
};

export default AnimationManager;
